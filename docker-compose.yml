services:
  booking:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/production.sqlite
      NODE_ENV: production
    volumes:
      - booking_data:/app/data
    restart: unless-stopped

volumes:
  booking_data:
